# Cursor指示書｜検索機能とイエローカード修正のプッシュ・本番デプロイ

対象リポジトリ: `CONV-FKT7-NEW-APP`（VIP Store PWA）
作成日: 2026-08-25

---

## 0. 現在の状態（確認済み）

```
ブランチ : main（origin/main より 2 コミット ahead）
未push   : c248174 feat(admin): ユーザー管理に会員番号・氏名・メールアドレスの検索を追加
           82cc847 fix(admin): イエローカードの色分けを全アバターに適用し、停止中会員を一覧に残す
現バージョン : version.json = 1.0.254
未追跡   : docs/ の指示書6本 ＋ scripts/check-duplicate-accounts.mjs
```

**⚠️ 今回のデプロイには、イエローカードの修正（82cc847）も一緒に含まれます。**
前回このコミットをデプロイせずに保留していたためです。**2つの変更がまとめて本番に出ることを理解したうえで進めてください。**

---

## 1. ⚠️ 先に確認：やらないこと

### バナーの露出強化（案C）は未実装のまま。実装しないこと

`docs/Cursor指示書_ペンギンモバイル_露出強化_2026-08.md`（ペンギンモバイルを1周に2回出す）は、
**指示書があるだけで実装されていません。今回も実装しません。**

```bash
git diff 9f91a68 HEAD --stat -- src/components/AffiliateBannerCarousel.tsx
# → 何も出なければ「未変更＝正しい状態」
```

### 重複アカウント調査の出力ファイルはコミットしないこと

`~/Desktop/fkt7-duplicate-check/` の CSV・サマリーには会員の氏名とメールアドレスが入っています。
**リポジトリの中に移動したりコミットしたりしないこと。**
`scripts/check-duplicate-accounts.mjs`（スクリプト本体）はコミットして構いません。

---

## 2. 事前チェック

```bash
git status -s          # 未追跡の docs 6本 と scripts 1本 以外は空であること
ls -la .env && npm run build
firebase login:list
```

> ⚠️ `npm run lint` は **既存の別ファイルのエラーで失敗します。それが現状の正常です。直さないこと。**

---

## 3. Step 1: 指示書とスクリプトをコミット

```bash
git add -A
git commit -m "docs(admin): 指示書6本と重複アカウント調査スクリプトを追加

- イエローカードのアイコン色（調査・修正・デプロイ）
- ペンギンモバイルのバナー露出強化（未実装）
- ユーザー管理の検索機能
- scripts/check-duplicate-accounts.mjs（読み取り専用の集計スクリプト）"
```

確認：

```bash
git status -s          # → 空
git log --oneline -4
```

**出力CSVがコミットに含まれていないことを必ず確認すること。**

```bash
git show --stat HEAD | grep -i "duplicate-check_"
# → 何も出ないのが正解（スクリプト check-duplicate-accounts.mjs は出てよい）
```

---

## 4. Step 2: プッシュ

```bash
git push origin main
```

3コミット（82cc847 / c248174 / 今回の docs コミット）が反映される。
`.github/workflows/` は空なので CI は走らない。**push だけではデプロイされない。**

---

## 5. Step 3: 本番デプロイ

```bash
npm run deploy
```

内訳: `version:bump`（1.0.254 → 1.0.255）→ `build` → `firebase deploy --project conv-fkt7-new-app`

`firebase deploy`（ターゲット無指定）は **hosting:pwa / hosting:job / functions / firestore rules・indexes / storage rules をすべて**デプロイする。
**作業ツリーがクリーンな状態で実行すること**（Step 3 のコミット後）。

### デプロイ後の確認

```bash
cat version.json    # → 1.0.255
```

- 本番 https://conv-fkt7-new-app.web.app を **シークレットウィンドウ** で開く
  （PWA の Service Worker キャッシュが残るため、通常タブでは旧版が出ることがある）
- アプリ内バージョン表示（`VersionBadge`）が **1.0.255**
- 求人サイト https://conv-fkt7-new-app-job.web.app が壊れていないこと
- **バナーは7枚のまま**（露出強化は未実装。ドットが8個ならおかしい）

---

## 6. Step 4: バージョン採番コミット → プッシュ

```bash
git add version.json
git commit -m "chore: デプロイに伴うバージョン 1.0.255"
git push origin main
```

> バージョン番号は `cat version.json` の **実際の値** を使う。1.0.255 と決め打ちしない。

---

## 7. デプロイ後にオーナーが確認すること（Cursor は報告に転記するだけ）

本番のシークレットウィンドウで管理画面にログインして確認する。

### ユーザー管理の検索（今回の主役）

- [ ] 検索ボックスが出ている
- [ ] `421` / `00421` / `#00421` / `#421` のいずれでも目的の会員が出る
- [ ] 姓だけ・名だけ・スペースあり・ひらがなでヒットする
- [ ] メールアドレスの一部で絞り込める
- [ ] 「稲葉 2002」のように2語入れると両方を含む会員だけが残る
- [ ] 件数表示が正しい（`全◯名` / `◯名中◯名を表示`）
- [ ] 該当なしのとき案内が出て、「検索をクリア」で戻る
- [ ] 検索した状態で列見出しをクリックすると、**絞り込んだ結果の中で**並び替わる
- [ ] 検索した状態で全選択すると、**表示中の人だけ**が選ばれる
- [ ] **何人か選択した状態で検索語を変えると、選択が解除され案内が出る**
- [ ] CSV出力ボタンに件数が出ていて、押すとその件数どおりに出力される
- [ ] 「会員番号を振る」の挙動が今までと変わっていない

### イエローカードのアイコン色（前回分。今回まとめて本番に出る）

- [ ] カード1枚 → **顧客リスト / チャットヘッダー / 吹き出し左 / 全チャット検索結果** の4箇所すべてで黄色
- [ ] 2枚 → 4箇所すべて橙
- [ ] 3枚 → 橙のまま顧客リストに残り、**「停止中」と表示される**
- [ ] 停止中の会員をタップして **過去のチャットが読める**
- [ ] 顧客管理タブで付与 → チャットタブに戻ると色が反映されている
- [ ] **クーポン配信画面の宛先に停止中の会員が混ざっていない**

### 共通

- [ ] Console / Network にエラーが出ていない

問題が見つかった場合は、**その場で直そうとせず**、症状を報告すること。

---

## 8. 最終確認

```bash
git status -s              # → 空
git log --oneline -5
git status -sb | head -1   # → ## main...origin/main（ahead / behind なし）
```

---

## 9. やってはいけないこと

- **バナー露出強化（案C）を「ついでに」実装する**（別タスクです）
- 重複アカウント調査の **出力CSV・サマリーをコミットする**
- `npm run lint` のエラーを直す（**既存の別問題**）
- `version.json` を手で編集する（`npm run deploy` が自動採番する）
- `firebase deploy` を別プロジェクトへ向ける／勝手に `--only` で絞る
- 本番確認で見つかった不具合をその場で修正してデプロイし直す（**まず報告**）
- `.env` をコミットする
- `git push --force` を使う

---

## 10. 完了報告に含めてほしいこと

- push したコミットのハッシュ（4本）
- デプロイ後の実バージョン番号
- **出力CSVがコミットに含まれていないことの確認結果**
- 本番でバナーが **7枚のまま**であることの確認結果
- 想定外の警告やエラーがあれば全文
