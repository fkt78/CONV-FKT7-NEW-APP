# Cursor指示書｜イエローカード修正のプッシュと本番デプロイ

対象リポジトリ: `CONV-FKT7-NEW-APP`（VIP Store PWA）
作成日: 2026-08-25
前提コミット: `82cc847 fix(admin): イエローカードの色分けを全アバターに適用し、停止中会員を一覧に残す`

---

## 0. 現在の状態

```
ブランチ : main（origin/main より 1 コミット ahead）
最新     : 82cc847（イエローカード修正。コード変更は完了・コミット済み）
現バージョン : version.json = 1.0.254
未追跡   : docs/ の指示書3本
  - Cursor指示書_調査_イエローカードのアイコン色_2026-08.md
  - Cursor指示書_修正_イエローカードのアイコン色_2026-08.md
  - Cursor指示書_ペンギンモバイル_露出強化_2026-08.md
```

このタスクのゴール：**未追跡の指示書をコミット → push → 本番デプロイ → バージョン採番コミットを push**

---

## ⚠️ 1. 先に確認：バナーの露出強化は「まだ入っていません」

`docs/Cursor指示書_ペンギンモバイル_露出強化_2026-08.md`（ペンギンモバイルを1周に2回出す案C）は、
**指示書があるだけで実装されていません。** `AffiliateBannerCarousel.tsx` は未変更です。

**このタスクでは、その実装は行わないでください。** 今回デプロイされるのは
**イエローカードのアイコン色の修正だけ**です。バナーの件は別タスクとして後日あらためて指示します。

念のため、着手前に未変更であることを確認してください。

```bash
git diff 9f91a68 82cc847 --stat -- src/components/AffiliateBannerCarousel.tsx
# → 何も出なければ「未変更＝正しい状態」
```

---

## 2. 事前チェック

```bash
# 作業ツリーの状態（未追跡の指示書3本以外は空であること）
git status -s

# ビルドが通るか（.env が必要）
ls -la .env && npm run build

# Firebase CLI にログイン済みか
firebase login:list
```

> ⚠️ `npm run lint` は **既存の別ファイルのエラーで失敗します。それが現状の正常です。**
> **このタスクで lint エラーを直そうとしないこと。**

---

## 3. Step 1: 指示書をコミット

```bash
git add -A
git commit -m "docs(admin): イエローカード修正の調査・修正指示書とバナー露出強化の指示書を追加"
```

確認：

```bash
git status -s        # → 空
git log --oneline -3
```

---

## 4. Step 2: プッシュ

```bash
git push origin main
```

`.github/workflows/` は空なので CI は走らない。**push だけではデプロイされない。**

---

## 5. Step 3: 本番デプロイ

```bash
npm run deploy
```

内訳: `version:bump`（1.0.254 → 1.0.255）→ `build` → `firebase deploy --project conv-fkt7-new-app`

`firebase deploy`（ターゲット無指定）は **hosting:pwa / hosting:job / functions / firestore rules・indexes / storage rules をすべて**デプロイする。
**作業ツリーがクリーンな状態で実行すること**（Step 3 のコミット後）。

### デプロイ後の確認

```bash
cat version.json    # → 1.0.255
```

- 本番 https://conv-fkt7-new-app.web.app を **シークレットウィンドウ** で開く
  （PWA の Service Worker キャッシュが残るため、通常タブでは旧版が出ることがある）
- アプリ内バージョン表示（`VersionBadge`）が **1.0.255**
- 求人サイト https://conv-fkt7-new-app-job.web.app が壊れていないこと
- **バナーは7枚のまま**（露出強化は未実装なので、ドットが8個になっていたらおかしい）

---

## 6. Step 4: バージョン採番コミット → プッシュ

```bash
git add version.json
git commit -m "chore: デプロイに伴うバージョン 1.0.255"
git push origin main
```

> バージョン番号は `cat version.json` の **実際の値** を使う。1.0.255 と決め打ちしない。

---

## 7. デプロイ後にオーナーが確認すること（Cursor は報告に転記するだけ）

本番のシークレットウィンドウで管理画面にログインし、以下を確認する。

- [ ] イエローカード1枚 → **顧客リスト / チャットヘッダー / 吹き出し左 / 全チャット検索結果** の4箇所すべてで黄色
- [ ] 2枚 → 4箇所すべて橙
- [ ] 3枚 → 橙のまま顧客リストに残り、**「停止中」と表示される**
- [ ] 停止中の会員をタップして **過去のチャットが読める**
- [ ] 取消（-1）で色が戻る
- [ ] 顧客管理タブで付与 → チャットタブに戻ると色が反映されている
- [ ] **クーポン配信画面の宛先に停止中の会員が混ざっていない**
- [ ] 「停止中」バッジがレイアウトを崩していない（会員名や累計金額の表示が折り返しで乱れていないか）
- [ ] Console / Network にエラーが出ていない

問題が見つかった場合は、**その場で直そうとせず**、症状を報告すること。

---

## 8. 最終確認

```bash
git status -s              # → 空
git log --oneline -4       # → version bump / docs / 82cc847 が並ぶ
git status -sb | head -1   # → ## main...origin/main（ahead / behind なし）
```

---

## 9. やってはいけないこと

- **バナー露出強化（案C）を「ついでに」実装する**（別タスクです）
- `npm run lint` のエラーを直す（**既存の別問題**）
- `version.json` を手で編集する（`npm run deploy` が自動採番する）
- `firebase deploy` を別プロジェクトへ向ける／勝手に `--only` で絞る
- 本番確認で見つかった不具合をその場で修正してデプロイし直す（**まず報告**）
- `.env` をコミットする（`.gitignore` 済み。**絶対にコミットしない**）
- `git push --force` を使う

---

## 10. 完了報告に含めてほしいこと

- push した2コミットのハッシュ
- デプロイ後の実バージョン番号
- 本番でバナーが **7枚のまま**であることの確認結果
- 想定外の警告やエラーがあれば全文
